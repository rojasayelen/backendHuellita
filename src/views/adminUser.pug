html
  head
    title Admin Usuarios
    style
      | table { border-collapse: collapse; width: 100%; }
      | th, td { border: 1px solid #ccc; padding: 8px; text-align: left; }
  body
    h1 Vista de Administración de Usuarios
    
    table
      thead
        tr
          th ID
          th Nombre
          th Apellido
          th Email
          th Tipo
          th Acciones
      tbody
        each usuario in usuarios
          tr
            td= usuario.id
            td= usuario.nombre
            td= usuario.apellido
            td= usuario.email
            td= usuario.tipo_usuario 
            td
              form(
                action=`/usuarios/deleteUsuario/${usuario.id}`
                method="POST"
              )
                input(type="hidden" name="_method" value="DELETE")
                button.btn-eliminar(
                  type="submit" 
                  onclick=`return confirm('¿Eliminar a ${usuario.nombre}?')`
                ) 🗑️ Eliminar

    a(href="/usuarios/admin") Volver a la lista general
